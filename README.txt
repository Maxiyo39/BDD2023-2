Nombre: Benjamin Ponce		Rol: 	202173615-8
	Max Rosado			201910503-5

Especificaciones del Programa:
Se deben ajustar las variables server y nombre_bd que aparecen en SQL, donde server es el nombre del equipo (nombre del servidor segun SQL) y nombre_bd es el nombre del database a trabajar.

Para escoger una de las 10 operaciones, se debe ingresar el numero de la operacion (indicados en el PDF de la tarea) y para cerrar el menu de operaciones y el programa, se debe ingresar un 0.

Se implementaraon trigger para la corrección de los nombres de los paises de las columnas HOST, CHAMPION, RUNNER_UP y TRIRD_PLACE en la tabla del summary, en la columna TEAM en la tabla que posee toda la informacion de los archivos csv de todos los años registradas. Además, se implementó un view en la operacion 10.

La correccion de los nombres de los paises se hizo tras hacer un analisis de los casos que aparecen en la columna TEAM de la tabla res_FIFA_WCS